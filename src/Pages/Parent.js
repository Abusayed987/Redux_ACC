import React from 'react';

const Parent = () => {
    return (
        <div>
            {/* Theme setup completed now  Don't change after 1 months  */}
        </div>
    );
};

export default Parent;