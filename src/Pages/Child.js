import React from 'react';

const Child = () => {
    return (
        <div>
            {/* Theme setup completed now  Don't change after 1 months  */}
        </div>
    );
};

export default Child;